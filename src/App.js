import { BrowserRouter as Router, Route, Switch } from "react-router-dom";
function App() {
  return (
    <Router>
      <Switch>
        <Route component={} path="" />
        <Route component={} path="" />
        <Route component={} path="" />
        <Route component={} path="" />
        <Route component={} path="" />
      </Switch>
    </Router>
  );
}

export default App;
